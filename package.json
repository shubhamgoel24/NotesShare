{
  "name": "notesshare",
  "version": "1.0.0",
  "description": "app to create notes",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "pm2 start server && pm2 serve -l 8080 -s client/build && pm2 list",
    "stop": "pm2 delete server && pm2 delete static-page-server-8080"
  },
  "author": "Shubham",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.24.0",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "ejs": "^3.1.6",
    "express": "^4.17.1",
    "mongoose": "^6.0.13",
    "pm2": "^5.2.0"
  }
}
